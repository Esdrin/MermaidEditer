# 更新日志（Changelog）

本项目的所有重要变更均记录在此文件中。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-08-25

### 新增

- **GitHub Actions 云构建**：推送 `v*` 标签自动在 Windows / Linux 双平台构建安装包并发布到 Releases
- **画布滚轮缩放**：鼠标滚轮以光标为锚点缩放画布（0.2×–8×），滑块与快捷键联动
- **空白拖拽平移**：按住画布空白处拖动平移视口，重绘后位置保持
- **画布铺满窗口**：画布自动铺满中央区域，窗口缩放画布跟随扩展，内容保持 1:1
- **界面缩放**：状态栏右下角「界面缩放」整体放大界面文字与图标（70%–200%，自动记忆）
- **双击箭头编辑标签**：双击连线可就地编辑标签（清空即删除），与节点双击编辑一致
- **文件管理器空状态**：未打开文件夹时面板居中显示引导提示
- **应用自定义图标**：窗口、任务栏、安装程序统一使用 `assets/icon.png`

### 修复

- 节点形状 token 错误（两端圆弧 / 数据库形状写反）导致源码无法解析
- mermaid 渲染失败残留错误容器显示在页面底部
- 画布负坐标节点被裁剪不显示（窗口放大后左上空白区放置的形状消失）
- 源码模式画布被压缩到固定范围（svg 按原始像素尺寸显示，超出可滚动）
- 属性面板颜色选择器 rgb 模式产生 Mermaid 不支持的格式导致渲染报错（统一 hex）
- 界面缩放下 Naive 下拉菜单定位错乱（弹出层不再 teleport 到 body）
- 节点文字色在 mermaid 下无法替换（节点颜色改由主题统一管理）

### 变更

- 删除属性面板中的颜色 / 线宽配置：颜色由主题统一管理，线宽固定 1px
- 删除自动化测试文件 smoke-test.js
- 项目接入 git 版本管理，`node_modules` / 构建产物不入库

## [1.0.0] - 2026-08-25

### 新增

- Visio 风格界面：文件 / 开始 / 插入 / 设计 / 视图 五页功能区（Naive UI）
- 自由画布模式：形状拖入、端口连线、连线改接、双击编辑、双向同步 Mermaid 源码
- 左侧文件管理器（树状）、外部实时同步、最近文件、草稿恢复
- 实时渲染、点击映射、快捷键、PNG / SVG / PDF / Markdown 导出
- 完全离线运行，不依赖网络
